# Local databases (never federated) + timestamped backups (never pruned)
*.db
*.db-journal
*.sqlite
*.sqlite3
backups/

# Generated per-user deliverables (the skill writes these locally)
app-intel-report.html

# Local config with the user's own settings/keys
config.json

# Secrets / env
.env
.env.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# OS
.DS_Store
Thumbs.db

# Background research (not part of the shipped project)
Open-source AppKittie alternatives*.md
open-app-intel-PRD.md

# Daily-run logs (append-only, local)
logs/
